#[defaults]
#inventory = inventory.ini
#private_key_file = ~/.ssh/id_ansible
#host_key_checking = False

[defaults]
# 🔹 Deprecation-Warnings deaktivieren
deprecation_warnings = False

# 🔹 SSH Key Check deaktivieren
host_key_checking = False

# 🔹 Standard-Inventory (falls du kein CLI-Argument angibst)
inventory = ./inventory_2.ini

# 🔹 Standard-SSH-Backend für Network-Geräte
# Network CLI ist schneller und stabiler für Cisco
connection = network_cli

# 🔹 Timeout und Retries für Network-Geräte
timeout = 30
retry_files_enabled = False

# 🔹 Konsistente Ausgabe
stdout_callback = default
log_path = ./ansible.log

# 🔹 Automatische Fact-Sammlung nur minimal
gathering = smart
fact_caching = jsonfile
fact_caching_connection = ./facts_cache
fact_caching_timeout = 3600
